import React from "react";
import { storiesOf } from "@storybook/react";
import Template from ".";

storiesOf("Template", module).add("Centralizer", () => (
  <Template.Centralizer>sample</Template.Centralizer>
));
